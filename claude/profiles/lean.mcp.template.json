{
  "$comment": "Sanitized MCP template for the lean Claude profile. The renderer substitutes ${HOME} at launch into a 0600 temp file and never touches live config. brain renders only for the work scope when ~/work/brain exists; otherwise the canonical memory path applies. Derived from mcp/servers.template.json (see docs/mcp-strategy.md).",
  "mcpServers": {
    "brain": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${HOME}/work/brain/_meta/mcp/server.mjs"
      ],
      "env": {
        "OBVAULT_ROOT": "${HOME}/work/brain"
      },
      "require_scope": "work"
    }
  }
}
