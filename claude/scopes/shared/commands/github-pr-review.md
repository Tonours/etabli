---
description: Compatibility alias for /pr-review
argument-hint: [PR URL/number/repo alias]
allowed-tools: [Read, Glob, Grep, AskUserQuestion]
---

# GitHub PR Review

User request: $ARGUMENTS

This command is a compatibility alias for `/pr-review`.

Resolve and follow the shared contract in `workflow/skills/pr-review.md` using
the same source-resolution order as `/pr-review`.
