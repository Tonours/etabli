interface:
  display_name: "Maintainer Orchestrator"
  short_description: "Coordinate repo queues and worker gates."
  default_prompt: "Use $maintainer-orchestrator to triage my repo queues, coordinate authorized worker threads, monitor gates, and prepare decision-ready next actions."

