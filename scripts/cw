#!/usr/bin/env bash

set -euo pipefail

resolve_script_dir() {
    local source_path="${BASH_SOURCE[0]}"
    local dir=""

    while [ -L "$source_path" ]; do
        dir="$(cd -P "$(dirname "$source_path")" >/dev/null 2>&1 && pwd)"
        source_path="$(readlink "$source_path")"
        [[ "$source_path" != /* ]] && source_path="${dir}/${source_path}"
    done

    cd -P "$(dirname "$source_path")" >/dev/null 2>&1 && pwd
}

SCRIPT_DIR="$(resolve_script_dir)"
# shellcheck source=lib/worktree-common.sh
source "${SCRIPT_DIR}/lib/worktree-common.sh"
# shellcheck source=lib/cw-worktree-state.sh
source "${SCRIPT_DIR}/lib/cw-worktree-state.sh"
# shellcheck source=lib/cw-tmux.sh
source "${SCRIPT_DIR}/lib/cw-tmux.sh"
# shellcheck source=lib/cw-cli-common.sh
source "${SCRIPT_DIR}/lib/cw-cli-common.sh"
# shellcheck source=lib/cw-cli-open.sh
source "${SCRIPT_DIR}/lib/cw-cli-open.sh"
# shellcheck source=lib/cw-cli-maintain.sh
source "${SCRIPT_DIR}/lib/cw-cli-maintain.sh"

COMMAND="${1:-}"
case "$COMMAND" in
    new|open|ls|attach|merge|rm|pick)
        shift
        "cw_cmd_${COMMAND}" "$@"
        ;;
    -h|--help|help)
        cw_usage
        ;;
    "")
        cw_usage >&2
        exit 1
        ;;
    *)
        cw_cmd_new "$@"
        ;;
 esac
