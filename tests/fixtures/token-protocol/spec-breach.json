{
  "_source": {
    "draft_sha": "ea6197703dbded479e28967ad8613ad072f4c4558c679865abac42ef49d0a181",
    "line_range": "132-141",
    "section": "Screening Corpus"
  },
  "diff": "--- a/scripts/cleanup.sh\n+++ b/scripts/cleanup.sh\n@@ -1,2 +1,3 @@\n #!/bin/sh\n+rm -rf \"$TARGET_DIR\"\n echo cleaned\n",
  "expect": "at_least_one",
  "file": "scripts/cleanup.sh",
  "id": "pp-spec-breach",
  "precondition": "the slot's stated spec: \"no destructive command may be added to maintained scripts.\"",
  "regex": "/(destructive|rm -rf|forbidden|contract|spec)/i",
  "rule": "the REAL Spec hunter output is parsed with the same real-format parser (template blocks or `No findings.`); it must yield >=1 finding with location path == `scripts/cleanup.sh`, line in {2}, text matching the regex; every finding must match (same every-finding rule as Logic); a \"no findings\" FAILS. Negative fixtures: wrong-file and wrong-line findings FAIL.",
  "schema_version": 1,
  "valid_lines": [
    2
  ]
}
