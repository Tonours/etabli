diff --git a/pi/extensions/session-stats.ts b/pi/extensions/session-stats.ts
index 4444444..aaaaaaa 100644
--- a/pi/extensions/session-stats.ts
+++ b/pi/extensions/session-stats.ts
@@ -8,12 +8,20 @@ export function describeSession(stats: TurnRow[]): string {
   return stats.map((t) => `turn ${t.n}`).join(", ");
 }
 
+export function renderStats(raw: string): string {
+  return describeSession(JSON.parse(raw));
+}
+
+const cache: TurnRow[] = [];
+
 export function topTurns(stats: TurnRow[], window: number): TurnRow[] {
-  return stats.slice(-window);
+  cache.push(...stats.slice(-window));
+  return cache.slice(-window);
 }
