# Fixture instructions

Use the installed Etabli workflow. The existing CHALLENGED plan and `app.txt` are protected and must remain byte-for-byte unchanged. Only `.workflow/challenged-no-mutation/events.jsonl` may be created.
