Implement the existing READY root `PLAN.md` using the active Etabli `plan-implement` workflow. Change `app.txt` from `mode=old` to exactly `mode=ready`, validate it with `test "$(cat app.txt)" = "mode=ready"`, review the final diff, archive the plan at `docs/plan/ready-implementation.md`, and remove root `PLAN.md`.

Record schema-v2 workflow events in `.workflow/ready-implementation/events.jsonl` for run `ready-implementation` only with `scripts/workflow-event append ready-implementation <event> <json-detail>`. Never author timestamps or ledger JSON manually, and invoke each event as its own Bash tool call. The ordered sequence must include `route_decided`, `plan_created`, `validation_run`, `review_completed`, then terminal `completed`. Do not change any other tracked file.
