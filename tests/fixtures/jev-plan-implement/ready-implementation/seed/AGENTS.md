# Fixture instructions

Use the installed Etabli workflow. Only `app.txt`, root `PLAN.md`, `docs/plan/ready-implementation.md`, and `.workflow/ready-implementation/events.jsonl` may be created or changed.
