# Implemented: cross-model routes aligned with Opus 5.5

## Metadata
- Archived: 2026-09-23
- Source plan: `PLAN.md` — align subagent and cross-model routes with Opus 5.5
- Source plan SHA-256: `9658e6889a111285466d4971f7e57274c8a50510e5ad7cdbb5e0401c6c4e7f3e`
- Status: IMPLEMENTED
- Commit / branch: uncommitted on `main`
- Workflow initiative: `opus-5-5-routes`

## Outcome
- The Claude subagents needed no change. `~/.claude/agents/*.md` are symlinks into `claude/scopes/shared/agents/`, and each one declares a model alias: `worker: opus`, `scout: sonnet`, `reviewer: sonnet`, `adversary: fable`. `~/.claude/settings.json` pins no model, so the `opus` alias already resolves to Opus 5.5.
- `workflow/runtime/adversary-model-policy.json`: the anthropic frontier entry now declares `claude-opus-5-5` with Pi route `cursor/claude-opus-5-5@1m`, and `policy_version` is `2026-09-23`. The declared model now matches what the `opus` route actually runs.
- `pi/agent/settings.json` now enables `cursor/claude-opus-5-5@300k` and `cursor/claude-opus-5-5@1m`, replacing Opus 5.
- The smoke frontier list and two `settings-consistency` assertions pin the new ids.

## Context
- `pi --list-models` (2026-09-23): cursor lists `claude-opus-5-5@1m/@300k`; github-copilot has no Opus 5.5.
- The policy sets `require_effective_model_provenance: true`. A stale declared model name misattributes cross-model reviews.

## Decisions
### Keep the Claude agents on aliases
- Context: Opus 5.5 was released.
- Choice: no change to the agent frontmatter.
- Rejected options: pinning versioned ids, which would need a manual edit at every release.
- Rationale: aliases track releases automatically; model diversity (sonnet and fable away from the opus main loop) is unchanged.
- Consequences: only the provider-specific pinned ids need an update per release.

## Accepted Drift
- Original plan/spec: three files.
- Implemented reality: four files, adding the `settings-consistency` assertions accepted from the plan adversary.
- Why accepted: those assertions strengthen the checks; before them, reverting half of the settings change went unnoticed.

## Validation Evidence
- `cd pi && bun test ./extensions/__tests__/settings-consistency.test.ts`: 6/6; the new assertion failed before the change.
- `bash tests/workflow-docs-smoke.sh`: pass, including the check that every Pi route is enabled.
- `env -u TYPESAFE_API_KEY scripts/verify-agentic-infra core`: 22/22.
- Reviews, all same-family (no non-Claude runner on this machine): plan adversary (fable) GO WITH NOTES; code review (sonnet) GO; code-diff adversary (fable) GO WITH NOTES.
- Event ledger `.workflow/opus-5-5-routes/events.jsonl`, validated with `--profile autonomous-completed`.

## Follow-up State
- Remaining risks: the Cursor Opus 5.5 route is listed in the catalogue but has not been exercised live; `pi-review-hunter` strips `--model` for `cursor/*` ids (pre-existing, caught by the provenance gate).
- Parking lot:
  - The Cursor IDE Task model `claude-opus-5-thinking-high`: change `workflow/skills/review.md:34`, `workflow/skills/pr-review.md:53` and `tests/workflow-docs-smoke.sh:568` together once the 5.5 name is verified.
  - Optionally move Pi `cursor/claude-fable-5` to `fable-5-1`.
  - Optionally retire the local `cursor/claude-opus-5@*` entries through `deployLegacyModels`.
- Superseded docs/specs: none.
- Next links: `workflow/runtime/adversary-model-policy.json`.
