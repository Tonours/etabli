# Residual risks — Etabli vNext (inspectable)

1. **Live effectiveness not verified (waived as goal stop condition).** User 2026-07-23: complete without LIVE_EVAL_BUDGET_USD. Mechanical gate still honest (`live-blocked.json` / `--live-status`). Live pass@1/pass^3 remain unmeasured until a positive budget is set later. See `goal-completion.json`.
2. **`pi.supports_goal_state` is `unknown`.** Session-scoped proof only; honest gap.
3. **Deterministic host suite ≠ multi-turn paid agent proof.** Offline control-plane floor only.
4. **Frozen corpus can be gamed** if sealed held-out or graders are later weakened.
5. **Environment side-effect:** accidental full `install-main` during diagnosis upgraded local brew packages.
6. **Candidate reject is offline comparative only.**

No P0 gap is silently unknown. Live is labelled not-verified + waived-for-completion, not silent green.
