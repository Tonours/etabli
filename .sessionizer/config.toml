# Repo-local Sessionizer layout for etabli itself
# Applied only when Sessionizer creates a new workspace for this repo/worktree.

[layout]
focus = "agent"

[tabs.dev]
label = "dev"

[[tabs.dev.panes]]
id = "shell"
title = "shell"
command = "/bin/zsh -l"

[[tabs.dev.panes]]
id = "agent"
from = "shell"
title = "agent"
split = "right"
ratio = 0.65
command = "pi"

[tabs.checks]
label = "checks"

[[tabs.checks.panes]]
id = "checks"
title = "checks"
command = "/bin/zsh -lc 'echo etabli checks:; ls scripts tests | head; exec /bin/zsh -l'"
